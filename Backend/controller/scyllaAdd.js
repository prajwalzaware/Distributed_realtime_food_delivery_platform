// import {  insertUser } from '../config/scyllaDb.js';

// async function scyllaAdd(req, res) {
//   const { name, descrition } = req.body;
//   try {
//     await insertUser(name, descrition);
//     res.status(201).json({ message: "User added successfully!" });
//   } catch (err) {
//     res.status(500).json({ error: "Failed to add user." });
//   }
    
// }

// export default scyllaAdd;
