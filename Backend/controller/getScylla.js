// import { getUsers } from '../config/scyllaDb.js';

// async function getScylla(req, res) {
//   try {
//     const users = await getUsers();
//     res.status(200).json(users);
//   } catch (err) {
//     res.status(500).json({ error: 'Failed to fetch users.' });
//   }
// }

// export default getScylla;
